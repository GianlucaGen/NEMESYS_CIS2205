@model aspnet_blog_application.ViewModels.HallViewModel

@{
    ViewData["Title"] = "Hall of Fame";
}
<h1>@ViewData["Title"]</h1>

<div class="row">
    @foreach (var entry in Model.Entries)
    {
        <div>
	@entry.Name - @entry.NumReports
	</div><br>
    }
</div>